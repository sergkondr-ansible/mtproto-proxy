---

galaxy_info:
  author: Sergey Kondrashov
  description: The role sets up mtproto-proxy server(proxy for telegram)
  min_ansible_version: 2.4.2
  github_branch: master
  platforms:
    - name: Ubuntu
      versions:
        - xenial

dependencies:
  - src: git@github.com:sergkondr-ansible/docker.git
    name: docker
    path: roles
    scm: git
    version: master
